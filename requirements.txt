# Runtime
mcp>=1.0              # MCP stdio transport API stable since 1.x
sqlite-vec>=0.1.6     # vec_distance_cosine() and virtual table support
httpx>=0.27.0         # async client used for Ollama/OpenAI calls
fastapi>=0.110.0      # lifespan context manager, BaseHTTPMiddleware
uvicorn>=0.30.0       # ASGI server
jinja2>=3.1.0         # template rendering for admin UI
python-dotenv>=1.0.0  # .env loading

# Test
pytest>=8.0
pytest-asyncio>=0.21  # asyncio_mode=auto introduced in 0.21
pytest-httpx>=0.30.0  # async httpx test client
